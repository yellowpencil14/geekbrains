numbers = [2, 5, 13, 7, 6, 4] #заданный массив
size = len(a) #размер заданного массива лучше вычислить функцией, чем вводить вручную
index = 0 #индекc массива, счетчик для цикла while
sum = 0 #сумма элементов массива
avg = 0 #этого можно не делать, т.к. эта переменная объявляется дальше
while index < size:
    sum += numbers[index]
    index += 1
avg = sum / size #среднее арифметическое массива
print("Среднее арифметическое равняется " + str(avg)) #ответ: Среднее арифметическое равняется 6.1666666667